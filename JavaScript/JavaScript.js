// script.js
document.getElementById('sobremim').addEventListener('click', function() {
    // Define a altura para rolar (em pixels)
    const scrollToHeight = 800;

    // Rolagem suave até a altura desejada
    window.scrollTo({
        top: scrollToHeight,
        behavior: 'smooth'
    });
});

// script.js
document.getElementById('atendimento').addEventListener('click', function() {
    // Define a altura para rolar (em pixels)
    const scrollToHeight = 800;

    // Rolagem suave até a altura desejada
    window.scrollTo({
        top: scrollToHeight,
        behavior: 'smooth'
    });
});

// script.js
document.getElementById('clinica').addEventListener('click', function() {
    // Define a altura para rolar (em pixels)
    const scrollToHeight = 800;

    // Rolagem suave até a altura desejada
    window.scrollTo({
        top: scrollToHeight,
        behavior: 'smooth'
    });
});

// script.js
document.getElementById('consultorio').addEventListener('click', function() {
    // Define a altura para rolar (em pixels)
    const scrollToHeight = 800;

    // Rolagem suave até a altura desejada
    window.scrollTo({
        top: scrollToHeight,
        behavior: 'smooth'
    });
});

// script.js
document.getElementById('contato').addEventListener('click', function() {
    // Define a altura para rolar (em pixels)
    const scrollToHeight = 800;

    // Rolagem suave até a altura desejada
    window.scrollTo({
        top: scrollToHeight,
        behavior: 'smooth'
    });
});

